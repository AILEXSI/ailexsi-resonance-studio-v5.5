# AILEXSI Resonance Studio V5.6 — Release

**Status:** **HUMAN-PROVEN** 2026-09-18. Ready to consolidate into `main`.

Version **5.6.0**. Toolbar chip **`V5.6.0`**. JSON `schemaVersion` **5**. PRE-AI. **Not V6.**

One-look table: `CURRENT.md`. V5.5 media/export proofs: `docs/V5.5-HUMAN-ACCEPTANCE.md`.

Coordinator: merge this stack tip to `main`, close superseded drafts **#27–#34**, build final `AILEXSI Resonance Studio V5.6.exe`. Agent does **not** merge.

## What V5.6 is

V5.5 media baseline (AILEXSI Frame Engine, ENC-01 1080p, D–H production surface) plus workspace / VIS / header finalization:

| Surface | What shipped |
| --- | --- |
| **LEXI family** | Seven FLOW scenes, separately selectable: `lexi` · `lexi-ref` · `lexi-2036` · `lexi-v3` · `lexi-v2` · `lexi-minimal` · `lexi-v1`. Hierarchical browser (ALL / LEXI / CLASSIC → family → scene). Empty future families stay visible and empty. Catalog is `SCENE_CATALOG` only. |
| **VIS menu** | VIS styles overlay: click applies through the same rematerialize path as cycle (`applyPickVisualizerScene`). Title-bar drag, viewport clamp, session persist. Short arranger / bottom-clip opens further up. Mute **M** does not open the menu. |
| **UI Finalization** | Inspector OPEN/CLOSED fold (existing Preview↔Inspector h-split). Timeline Focus on the existing Preview/Arrange splitter (stores/restores the exact prior divider). ARRANGE/CUTTER segmented look. Transport grouping via separators. No new shortcuts. Mixer product semantics unchanged. |
| **Track headers** | Progressive overflow only — no Track Density modes, no wrap, no crushed buttons. Identity + Mute stay direct. Semantic divider floor **80px** (`headerUsableMinPx()` = `LANE_LABEL_MIN_PX`). Compact glyphs stay M/S/W/VOL. Overflow menu uses readable Resonance labels (Solo, Write automation, Volume automation, Chapter group) and live `.active` / `aria-pressed` / group value. |
| **Overflow popup** | Below the trigger if it fits; else flip above; else taller side + `maxHeight` + internal scroll. Horizontal shift reuses VIS overlay `clampVisBrowserPos` / 8px margin. Portaled like the VIS overlay so lane/timeline clip cannot hide actions. Repositions on scroll/resize. Operator: *perfect sauber gelöst*. |

## What did not change

- Mixer channel DSP / routing / fader model
- AILEXSI Frame Engine scheduling, watermarks, escapes
- Project `schemaVersion` **5**
- AppData identifier `com.ailexsi.resonance-studio-v5-5` and `resonance-studio-v5-5*` keys
- Production Pass **I–N** and Future UI zettel (still **PLANNED / NOT IMPLEMENTED**)
- No V6. No AI/MCP product surface.

## Protected baselines (already documented — no new numbers)

**VIS-RESPONSE-02** remains HUMAN-PROVEN 2026-09-17 (M.G.M. *perfect*). Locked defaults: gain **1.25** / gamma **0.68** / spread **18** / transient **0.38**; shared `scene-impact.ts`; no AGC; Preview = Export.

This V5.6 stamp does **not** invent new long-form timings. Cite only what is already on file:

| Baseline | Documented figure | Where |
| --- | --- | --- |
| VIS-SYNC-01 long-form | **~34:18** and **~64 min** VIDEO+VIS+AAC | `CURRENT.md` · `docs/compliance/VIS-RESPONSE-02-KICK-MID.md` |
| AUDIO-01 | **~29:11** 1080p30 VIDEO+VIS+AUDIO | `CURRENT.md` · `docs/V5.5-HUMAN-ACCEPTANCE.md` |
| ENC-01 | 1920×1080 H.264 @ **24 / 25 / 30** (`0ec7758`) | `CURRENT.md` · `docs/V5.5-HUMAN-ACCEPTANCE.md` |
| VIS-RESPONSE-01 | ~6 min Lattice 1080p30+AAC soft-PASS | `docs/compliance/VIS-RESPONSE-01-IMPACT-LAYER.md` |
| Chrome fixture | **90/90/90** VIDEO→VIS→VIDEO | AUTOMATED-TESTED only — not a minute count |

## Lineage (consolidate these, then close drafts)

| PR | Branch | Role |
| --- | --- | --- |
| **#32** | `cursor/vis-lexi-family-consolidation-0a86` | LEXI FLOW library + VIS browser; later menu apply/drag |
| **#33** | `cursor/ui-workspace-ergonomics-v56-11df` | UI Finalization + product identity **5.6.0** |
| **#34** | `cursor/ui-responsive-track-headers-6a5d` | Progressive overflow, 80px semantic min, readable menu, popup contain — **merge tip** |

Superseded intermediate LEXI drafts **#27–#31** and the stacked #32–#34 drafts close after the tip lands on `main`.

## Identity

| | |
| --- | --- |
| Product | AILEXSI Resonance Studio V5.6 |
| npm / chip | **5.6.0** |
| Tauri window / EXE | `AILEXSI Resonance Studio V5.6` / `AILEXSI Resonance Studio V5.6.exe` |
| Frame Engine | **AILEXSI** |
| Schema | **5** |

## Verification

MODE A (this tip): `tsc --noEmit` clean. UI/layout/timeline/inspector/mixer/v55 suite **340 passed** after the overflow-popup pass. Engine files not touched in the header/overflow work.

MODE B final EXE is the coordinator build after merge — not produced by this agent.
