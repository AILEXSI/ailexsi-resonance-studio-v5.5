# V5.5 human acceptance (MODE B)

**HUMAN-PROVEN** only from a named Windows EXE SHA that the operator drives.

V5 MODE B evidence (`docs/ACCEPTANCE.md`, `docs/exe-acceptance-2026-09-13.png`) is **historical V5** (chip `5.0.0`, Mediabunny export default). Do not treat it as V5.5 acceptance.

## 2026-09-16 MODE B EXE (operator PASS — AILEXSI export)

| | |
| --- | --- |
| Result | **PASSED** (operator confirmed) |
| Feature tip | `a01576b` (PR **#17**, merged to main) |
| Chip | **5.5.0** · schema **5** |
| Frame Engine | **AILEXSI** — Mediabunny-free |
| Proven | Windows WebView2 export, including VIS-mix / multi-clip; AFE-25 `prefer-software` EOF tail (CASE B); stall/export-fail dump identity (`productVersion` / `gitSha`) |
| Not claimed | Chrome 90/90/90 and pixel harnesses stay AUTOMATED-TESTED. Licensing not HUMAN-PROVEN. MODE A suite still has the pre-existing AFE-15 A/N dump-ban (2 failures). 1080p is **not** claimed on this `a01576b` tip — see ENC-01 `0ec7758` below. |

## Operator checklist

1. Build on Windows: `BUILD_AND_RUN_V5.5.cmd` or `npm run tauri:exe`.
2. Confirm Task Manager process **AILEXSI Resonance Studio V5.5**.
3. Confirm toolbar chip is exactly **`V5.5.0`** (top-right).
4. Open Export. Confirm **`Frame Engine: AILEXSI`**.
5. Export a 720p30 timeline to MP4. Confirm **Fertig** + `Exported … bytes`.
6. Confirm a failed/unreadable clip surfaces a typed **`AFE_*`** error — export must not silently paint HTMLVideo frames.
7. Confirm preview still plays HTMLVideo.
8. Confirm V5 AppData is untouched (`com.ailexsi.resonance-studio-v5`). V5.5 uses `com.ailexsi.resonance-studio-v5-5` and `resonance-studio-v5-5*` keys.
9. **ENC-01 1080p — HUMAN-PROVEN** on EXE tip `0ec7758` (PR **#18**). Operator confirmed real Windows WebView2 **1920×1080 H.264** @ **30 / 25 / 24 fps** (Fertig). Screenshot (30 fps): `docs/enc-01-1080p-acceptance-2026-09-16.png`. AFE remains closed.
10. **AFE-04 B-frame / varying CTTS — HUMAN-PROVEN** on the 2026-09-16 Windows EXE (`a01576b` lineage). Real production H.264 with B-frames / CTTS exported as **`Frame Engine: AILEXSI`**, no Mediabunny.
11. **STRESS-01 OPEN** — same physical `6C16E2CA-… - Kopie.mp4`, clip `sourceInMs 0` / `sourceOutMs 38600` on a long 1080p30 MODE B export. Confirm the second source frame (sample 3 / exact PTS **100000**) is encoded. Must **not** stall with `targetPtsSeen no` / `lastDecodedTs 500000` while later timestamps emit. ENC-01 1080p25/30 and AFE-25 prefer-software must still pass. **Not HUMAN-PROVEN until this EXE check.**
12. **STRESS-02 OPEN (diagnostic EXE)** — build with `$env:AILEXSI_DIAG_SOURCEMAP="1"; npm run tauri:exe` or `npm run tauri:exe:diag`. Repeat the **same ~25 min** stress export that failed on `fc7ab97` with only `Maximum call stack size exceeded`. On fail, scroll the Export failed-status and copy the full **STRESS-02 CALL-STACK DIAGNOSTIC** dump. Report `error.name` / `error.message` / original `error.stack`, **FIRST application frame**, **FIRST repeated function/frame**. Do not classify A–F and do not merge from this pass. Details: `docs/compliance/STRESS-02-CALL-STACK-DIAGNOSTIC.md`.

## 2026-09-16 MODE B EXE (operator PASS — ENC-01 1080p)

| | |
| --- | --- |
| Result | **PASSED** (operator confirmed) |
| Feature tip | `0ec7758` (PR **#18**, branch `cursor/enc-01-1080p-avc-capability-2f62`) |
| Chip | **5.5.0** · schema **5** |
| Frame Engine | **AILEXSI** — Mediabunny-free |
| EXE | `C:\Users\marti\ailexsi-resonance-studio-v5.5\AILEXSI Resonance Studio V5.5.exe` |
| SHA256 | `0FA47C1E71975D0480699EA972F8897193A247A4061FDA83B2B72B61DFF35678` |
| Proven | **1920×1080 H.264** export at **30 fps, 25 fps, and 24 fps**. 30 fps screenshot: Export dialog **Fertig** `Untitled_Resonance.v2.mp4`, Frame Engine **AILEXSI**, **1920×1080 / 30 fps**, long timeline IN ~00:00.40 · OUT ~06:59.93 (`docs/enc-01-1080p-acceptance-2026-09-16.png`). |
| Not claimed | Licensing. Chrome 90/90/90 and pixel harnesses stay AUTOMATED-TESTED. AFE remains **CLOSED** — this stamp is ENC-01 encoder capability only, not a new AFE ticket. |

## Windows EXE this bootstrap

Bootstrap agent did **not** build the EXE (Linux). Operator built locally. **2026-09-16 MODE B PASS** recorded above for AILEXSI export (`a01576b`) and ENC-01 1080p (`0ec7758`).

## Inherited V5 HUMAN-PROVEN (D–H)

D–H were HUMAN-PROVEN on V5 EXEs (see `CURRENT.md` / `docs/ACCEPTANCE.md`). V5.5 keeps that product surface. Those proofs are **V5 EXE SHAs**, not this V5.5 tree. Re-prove on a V5.5 EXE before claiming V5.5 HUMAN-PROVEN.
