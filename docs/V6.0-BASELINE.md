# AILEXSI Resonance Studio V6.0 — Baseline

**Status:** V6.0 began as an exact continuation of final HUMAN-PROVEN V5.6. This document is the known-good start stamp (`v6.0.0-baseline`), **not** a feature-complete claim.

No AI Director, MCP, providers, revision, transactions, clip merge, streaming mux, AUDIO-02, new VIS, or new UI. AFE / ENC / mux behavior is unchanged from the V5.6 freeze.

## Identity

| Field | Value |
| --- | --- |
| SOURCE_REPO | https://github.com/AILEXSI/ailexsi-resonance-studio-v5.6 |
| SOURCE_SHA (pre-freeze `main`) | `3b1b9511f86282c30a8d41fa7536d6cfd920e013` (PR **#37**) |
| V5.6 freeze tip / `v5.6.0-final` | `69f4b307fc9b5142a35fe096a814a43e3a07c6b3` |
| V6_REPO | https://github.com/AILEXSI/ailexsi-resonance-studio-v6.0 (intended PUBLIC active line; create+push if the GitHub App token cannot open a new user repo) |
| V6_INITIAL / parent SHA | `69f4b307fc9b5142a35fe096a814a43e3a07c6b3` (same commit as V5.6 freeze tip; full ancestry preserved) |
| V6_VERSION_COMMIT_SHA | `37a2cb929469f22658418973f0dae61787aae66e` |
| productVersion | **6.0.0** |
| PROJECT_SCHEMA_VERSION | **5** (not bumped) |
| Frame Engine | **AILEXSI** |
| npm | `@ailexsi/resonance-studio-v6.0` **6.0.0** |
| Tauri | productName `AILEXSI Resonance Studio V6.0` **6.0.0** |
| Cargo | `ailexsi-resonance-studio-v6-0` **6.0.0**, lib `ailexsi_resonance_studio_v6_0_lib` |
| Toolbar chip | **`V6.0.0`** |

V5.6 historical docs (`docs/V5.6-FINAL-FREEZE.md`, `docs/V5.6-RELEASE.md`, `docs/V5.5-HUMAN-ACCEPTANCE.md`, `docs/ACCEPTANCE.md`, `docs/compliance/`) keep their original V5.5/V5.6 numbers. Do not rewrite them.

PR **#36** AI-0 recon was **not** merged and was **not** cherry-picked into this baseline.

## AppData / bundle identifier (V6-specific)

| | V5.6 (frozen, untouched) | V6.0 (active) |
| --- | --- | --- |
| Tauri identifier / AppData folder | `com.ailexsi.resonance-studio-v5-5` | `com.ailexsi.resonance-studio-v6-0` |
| localStorage / IndexedDB prefix | `resonance-studio-v5-5*` | `resonance-studio-v6-0*` |

V6 does **not** migrate or overwrite V5.6 prefs. Project JSON `schemaVersion` **5** still opens (same `PROJECT_SCHEMA_VERSION`).

## Gates (Linux VM, identity tip `37a2cb9`)

| Gate | Result |
| --- | --- |
| `tsc --noEmit` | **PASS** (exit 0) |
| Focused identity (`tests/v55/identity.test.ts`, `afe-build-identity`, toolbar chip, track-header version) | **46 / 46 PASS** |
| Full `vitest run` | **1389 passed / 6 failed / 1395** in 163 files |
| Inherited AFE-15 dump-ban | **2** failures (`tests/export/afe-15-exact-pts-tail-ownership.test.ts` A + N) — present on V5.6 `main`, not a migration regression |
| Inherited STRESS-03 missing clip | **4** failures (`tests/export/stress-03-physical-source.test.ts` H–K) — operator clip not in this VM, not a migration regression |
| `vite build` | **PASS** — vite 7.3.6, 166 modules |
| Tauri Windows EXE | **NOT RUN HERE** — this environment is Linux; it cannot produce the Windows WebView2 EXE |

## Windows EXE recipe (coordinator / Windows host)

Do **not** commit `*.exe` (`.gitignore` excludes them). Preferred delivery: one GitHub Release asset on `v6.0.0-baseline`.

On a Windows machine with Node.js LTS, rustup, and WebView2:

```
git clone https://github.com/AILEXSI/ailexsi-resonance-studio-v6.0
cd ailexsi-resonance-studio-v6.0
git checkout v6.0.0-baseline
npm ci
npm run tauri:exe
```

Or double-click `INSTALL_BUILD_RUN_V6.0.cmd` / `BUILD_AND_RUN_V6.0.cmd`.

Expected output (not produced here):

| Field | Value |
| --- | --- |
| EXE_NAME | `AILEXSI Resonance Studio V6.0.exe` |
| EXE_PATH | repo root after `scripts/copy-exe.ps1` |
| EXE_SHA256 | **PENDING** Windows host build |
| GitHub Release | attach that single current V6 EXE; no Git LFS |

`OLD_EXE_COUNT_IN_V6` = **0**. `CURRENT_EXE_COUNT_IN_V6` = **0** (none tracked; none generated in this VM).

## Known inherited limitations

| Item | Status |
| --- | --- |
| **~160 min mux ArrayBuffer allocation failure** | Inherited only. **NOT HUMAN-PROVEN.** In-memory `muxAvcToMp4` + `new Uint8Array(bytes.byteLength)` copy can fail to allocate for a very large MP4. Do not invent 120/160 min HUMAN-PROVEN. |
| AUDIO-02 / 2 h streaming | **NOT IMPLEMENTED.** 120 min mix PCM remains RED in AUDIO-01 notes. |
| HUMAN-PROVEN long-form (do not invent new figures) | **~34:18**, **~64 min**, **~90 min (01:30:30)** VIDEO+VIS+AAC; ENC-01 **1080p24/25/30**; AUDIO-01 **~29:11** |
| Production Pass I–N | **PLANNED / NOT IMPLEMENTED** |
| Licensing | No `LICENSE` / `THIRD_PARTY_NOTICES`. Not MPL FREE. Not HUMAN-PROVEN. |

## Binary policy

- `.gitignore` excludes `*.exe` and named V5 / V5.5 / V5.6 / V6.0 root EXEs.
- No Git LFS in this lineage.
- Old experimental EXEs must not enter the V6 source tree.

## History

Full git history was taken from V5.6 `main` (bare/mirror), not from a ZIP or an anonymous squash. V6 `main` parent equals the V5.6 freeze tip. Ancestry includes `3b1b951`. Unmerged PR **#36** is not `main`.
